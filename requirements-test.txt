# Test-only requirements — excludes aider-chat (fully mocked in tests)
# and uses the same versions as the main requirements where applicable
python-telegram-bot==21.7
litellm==1.55.3
pyyaml==6.0.2
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24
