# Test-only requirements — excludes aider-chat (fully mocked in tests)
python-telegram-bot>=21.7
litellm>=1.55.0
pyyaml>=6.0
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24
pre-commit>=3.5.0
ruff>=0.3.0
