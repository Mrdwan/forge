[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q --tb=short"

[tool.coverage.run]
source = ["src", "forge_init"]
branch = true
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
fail_under = 100
